extend type Mutation {
  subscribeWebPush(key: String!, auth: String!, endPoint: String!): Boolean!
  unsubscribeWebPush(key: String!): Boolean!
  sendPushToAll(title: String!, content: String!, url: String!): Boolean!
}
